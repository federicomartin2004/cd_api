nelmio_api_doc:
    documentation:
        info:
            title: My cd API
            description: Este es el RESTful API para Cloud District
            version: 1.0.0
    routes: # to filter documented routes
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
